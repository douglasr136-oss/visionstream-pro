# .env.example - Modelo para variáveis de ambiente
# Copie este arquivo para .env e preencha os valores
# NÃO faça commit do arquivo .env real no GitHub!

API_KEY=sua_chave_secreta_muito_forte_aqui_123456
NODE_ENV=production
PORT=10000

# URLs permitidas para CORS (separadas por vírgula)
ALLOWED_ORIGINS=https://visionstream-app.onrender.com,http://localhost:3000
